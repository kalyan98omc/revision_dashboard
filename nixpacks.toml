[phases.setup]
nixPkgs = ["python311", "file"]

[phases.install]
cmds = ["cd Backend && pip install -r requirements.txt"]


[start]
cmd = "cd Backend && gunicorn wsgi:application --worker-class eventlet -w 1 --bind 0.0.0.0:$PORT --timeout 120"
[variables]
PYTHONUNBUFFERED = "1"
